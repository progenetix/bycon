import React from 'react';
export default function UriField({ value, prefix, name, }: {
    value: {
        uri: string;
        baseUri?: string;
    };
    name: string;
    prefix: string[];
}): React.JSX.Element;
