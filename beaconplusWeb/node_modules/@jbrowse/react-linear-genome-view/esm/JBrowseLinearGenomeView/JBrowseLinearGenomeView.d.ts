import React from 'react';
import { ViewModel } from '../createModel/createModel';
declare const JBrowseLinearGenomeView: ({ viewState, }: {
    viewState: ViewModel;
}) => React.JSX.Element;
export default JBrowseLinearGenomeView;
