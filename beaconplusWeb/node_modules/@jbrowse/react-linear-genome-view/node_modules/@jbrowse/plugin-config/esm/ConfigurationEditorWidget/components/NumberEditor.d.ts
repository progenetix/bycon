import React from 'react';
declare const NumberEditor: ({ slot, }: {
    slot: {
        name?: string;
        value: string;
        description?: string;
        set: (val: number) => void;
        reset?: () => void;
    };
}) => React.JSX.Element;
export default NumberEditor;
