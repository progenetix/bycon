import React from 'react';
declare const DisplayError: ({ model, }: {
    model: {
        renderProps: {
            radius: number;
        };
        error: unknown;
    };
}) => React.JSX.Element;
export default DisplayError;
