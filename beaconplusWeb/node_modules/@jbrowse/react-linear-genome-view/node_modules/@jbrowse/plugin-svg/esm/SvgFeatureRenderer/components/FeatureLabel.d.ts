import React from 'react';
import { Feature, Region } from '@jbrowse/core/util';
import { DisplayModel } from './util';
interface ViewParams {
    start: number;
    end: number;
    offsetPx: number;
    offsetPx1: number;
}
declare const FeatureLabel: ({ text, x, y, region, reversed, bpPerPx, exportSVG, feature, viewParams, color, fontHeight, featureWidth, allowedWidthExpansion, displayModel, }: {
    text: string;
    x: number;
    y: number;
    color?: string;
    fontHeight?: number;
    featureWidth?: number;
    bpPerPx: number;
    allowedWidthExpansion?: number;
    feature: Feature;
    reversed?: boolean;
    displayModel?: DisplayModel;
    exportSVG?: unknown;
    region: Region;
    viewParams: ViewParams;
}) => React.JSX.Element | null;
export default FeatureLabel;
