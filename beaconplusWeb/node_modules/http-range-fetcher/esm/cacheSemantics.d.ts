export declare function parseCacheControl(field: any): {};
export declare class CacheSemantics {
    constructor({ minimumTTL }: {
        minimumTTL: any;
    });
    calculateChunkExpirationDate(chunkResponse: any): Date | undefined;
    _coerceToDate(thing: any): Date | undefined;
    /**
     * check whether a cached chunk response is still valid and can be used
     * @param {object} chunkResponse
     * @returns {boolean}
     */
    cachedChunkIsValid(chunkResponse: any): boolean;
    /**
     * check whether the response for this chunk fetch can be cached
     * @param {object} chunkResponse
     * @returns {boolean}
     */
    chunkIsCacheable(): boolean;
}
