defaults:
  dataset_ids:
    - progenetix
  method: details
  response_type: AnalysisResponse
  h->o_access_key: "callsets._id"
bycon_definition_files:
  - config
  - beacon_info
  - beacon_mappings
  - dataset_definitions
  - filter_definitions
  - geoloc_definitions
  - variant_definitions
  - handover_definitions
meta:
  returned_schemas:
    Individual: https://progenetix.org/services/schemas/Callset/
  info: >
    The main individuals payload can be accessed in `response.results`.
response_types:
  - BiosampleResponse
  - AnalysisResponse
  - IndividualResponse
  - VariantInSampleResponse
methods:
  ids:
    - id
  details:
    - id
    - description
    - external_references
    - statusmaps
    - info
