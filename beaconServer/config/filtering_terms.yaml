bycon_definition_files:
  - config
  - beacon_mappings
  - dataset_definitions
  - filter_definitions
defaults:
  method: ""
  response_type: info
  stats_number: 1
meta:
  beacon_id: org.progenetix
  response_type: info
  returned_schemas:
    FilteringTerm: "https://beacon-project.io/schemas/beacon-v2/current/FilteringTerm.json"
  info: >
    The filtering terms can be accessed in `result_sets`.
methods:
  counts:
    - counts
  filtering_terms:
    - counts
    - filtering_terms
