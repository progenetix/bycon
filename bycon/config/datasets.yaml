bycon_definition_files:
  - config
  - dataset_definitions
  - filter_definitions
defaults:
  method: ""
  response_type: return_dbstats
  stats_number: 1
meta:
  beacon_id: org.progenetix
  response_type: InfoResponse
  returned_schemas:
    BeaconDataset: "https://beacon-project.io/schemas/beacon-v2/current/BeaconDataset.json"
  info: >
    The latest dbstats payload can be accessed in `response.results`.
methods:
  counts:
    - counts
  filtering_terms:
    - counts
    - filtering_terms
