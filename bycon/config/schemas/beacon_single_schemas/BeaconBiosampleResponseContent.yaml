"$schema": https://json-schema.org/draft/2020-12/schema
"$id": https://progenetix.org/services/schemas/BeaconBiosampleResponseContent/v2021-03-09
title: BeaconBiosampleResponseContent
meta:
  $ref: 'Beacon.yaml#/meta'
description: |
  Description pending
type: object
required:
  - exists
  - results
properties:
  exists:
    description: |
      Indicator of whether any biosample was observed in any of the
      datasets queried. This should be non-null, unless there was an
      error, in which case `error` has to be non-null.
    type: boolean
  error:
    $ref: 'BeaconError.yaml#/properties'
  numTotalResults:
    type: integer
    minimum: 0
  results:
    type: array
    items:
      $ref: 'BeaconBiosampleResponseResults.yaml#/'
  info:
    type: object
  resultsHandover:
    type: array
    items:
      $ref: 'BeaconHandover.yaml#/properties'
  beaconHandover:
    type: array
    items:
      $ref: 'BeaconHandover.yaml#/properties'
