"$schema": https://json-schema.org/draft/2020-12/schema
"$id": https://progenetix.org/services/schemas/BeaconVariantInSampleFields/v2021-03-07
title: BeaconVariantInSampleFields
meta:
  $ref: 'Beacon.yaml#/meta'
description: |
  Fields to filter by variant in sample.
type: object
properties:
  variantId:
    description: |
      `variantId` of the variant(s) in sample to display.
    type: string
  analysisId:
    description: |
      `analysisId` of the variant(s) in sample to display.
    type: string
  biosampleId:
    description: |
      `biosampleId` of the variant(s) in sample to display.
    type: string
