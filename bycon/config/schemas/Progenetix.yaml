title: Progenetix
description: >
  This is the root schema for the Progenetix resource, corresponding to the principle elements of the core data schema.
definitions:
  id: "#root"
  biosample: 
    $ref: Biosample.yaml#/properties
  individual:
    $ref: Individual.yaml#/properties
  callset:
    $ref: Callset.yaml#/properties
  variant:
    $ref: Variant.yaml#/properties
