# This file (re-)defines the dataset specific authorizations
anonymous:
  default: record
mbaudis:
  default: record
  progenetix: record
  cellz: record
  examplez: record
testuser:
  examplez: count    
