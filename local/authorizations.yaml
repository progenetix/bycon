# This file (re-)defines the dataset specific authorizations
# These definitions are for now kept in this separate file for testing and
# overall transparency
anonymous:
  default: record
mbaudis:
  default: record
  progenetix: record
  cellz: record
  examplez: record
  refcnv: record
testuser:
  examplez: count    
