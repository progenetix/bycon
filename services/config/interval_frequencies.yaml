defaults:
  method: frequencies
  response_entity_id: interval_frequencies
  frequencymaps_coll: frequencymaps
meta:
  info: >
    The main collations payload can be accessed in `response.results`.
integer_keys:
  - count
  - code_matches
